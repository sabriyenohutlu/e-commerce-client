
const MyProfilePage = () => {
   // API'den veri çekmek için fetch kullanıyoruz
  //  const res = await fetch('http://localhost:5000/api/products', {
  //   next: { revalidate: 60 }, // 60 saniyede bir tekrar veri çekme
  // });

 // const products = await res.json(); // JSON olarak veriyi al
  return (
    <div>MyProfilePage</div>
  )
}

export default MyProfilePage